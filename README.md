# GlassdoorInterviewScrapper


Change the `companyName` and `interviewPageUrl` variables and run the below command.

```bash
GLASSDOOR_EMAIL='mail@mail.com' GLASSDOOR_PASSWORD='password' node scrape.js
```
